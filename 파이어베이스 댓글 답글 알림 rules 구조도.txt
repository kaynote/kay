people (컬렉션)
 └ {postId} (문서)
     ├ comments
     │   └ {commentId}
     │        ├ replies
     │        └ (댓글 데이터)
     │
     ├ notifications        ← 사용자 알림
     │   └ {notificationId}
     │
     └ adminNotifications   ← 관리자 알림
         └ {notificationId}